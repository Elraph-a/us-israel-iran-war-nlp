# --- Runtime (serving the API) ---
fastapi>=0.110
uvicorn[standard]>=0.29
nltk>=3.8
textblob>=0.17
joblib>=1.3
# Pinned so the committed joblib models load correctly (they were fit with these):
scikit-learn==1.8.0
scipy==1.17.0
numpy==2.4.2

# --- Only needed to re-run precompute.py locally (source data is not in the repo) ---
pandas>=2.0
openpyxl>=3.1
emoji>=2.0
langdetect>=1.0.9
